# Redaction is the only control between customer personal data and the index. This is the
# second check on it, by different code: the pipeline refuses to index a batch that still
# matches identifier patterns, and this tests what actually reached the index.
id: i-other-customers-probe
class: pii
fixture: pii-other-customers-probe.json
market: GLOBAL
expect:
  no_pii: true
